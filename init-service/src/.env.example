S3_BUCKET=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_ENDPOINT=
# Shared with orchestrator-simple - must be the exact same value in both
JWT_SECRET=
# Shared with orchestrator-simple - must point at the same file (see src/db.ts)
OWNERSHIP_DB_PATH=